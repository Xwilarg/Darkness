{
  "devDependencies": {
    "minify": "^9.1.0",
    "mkdirp": "^1.0.4"
  },
  "scripts": {
    "build": "mkdirp js/ && minify src/Game.js > js/Game.js && minify src/init.js > js/init.js",
    "serve": "npm run build && php -S localhost:8182"
  }
}
