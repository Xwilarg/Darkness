{
  "scripts": {
    "build": "mkdirp js/ && esbuild src/Game.js --bundle --minify --outfile=js/Game.js && esbuild src/init.js --bundle --minify --outfile=js/init.js",
    "serve": "npm run build && php -S localhost:8182",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^8.33.0",
    "ts-loader": "^9.4.2",
    "typescript": "^4.9.5",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
